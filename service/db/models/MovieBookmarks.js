// const mongoose = require("mongoose");
// import { movie_schema } from "../schemas/movie";

// const MovieBookmarkSchema = new mongoose.Schema(movie_schema, { timestamps: true });

// export default mongoose.models.Bookmarks || mongoose.model("Bookmarks", MovieBookmarkSchema);